# 第17章 最佳实践
## 17.1 为列表渲染设置属性key
v-for使用key，因为这个key主要用在虚拟DOM算法中，对比新旧虚拟节点时辨识虚拟节点，在查找的时候会快一点。
## 17.2 在v-if/v-if-else/v-else中使用key
如果一组v-if+v-eles的元素类型相同，最好使用属性key（比如两个<div>元素）
<div
  v-if="error"
  key="serch-states"
>
  错误：{{error}}
</div>
<div
  v-else
  key="serch-states"
>
  {{result}}
</div>

## 17.3 路由切换组件不变
当页面切换到同一个路由但不同参数的地址时，组件的生命周期钩子并不会重新触发。
例如：
const routes = [
  {
    path: '/detail/:id',
    name: 'detail',
    component: Detail
  }
]
当我们从路由/detail/1切换到/detail/2时，组件没有发生任何变化。这是因为vue-router会识别出两个路由使用的是同一个组件从而进行复用，并不会重新创建组件，因此组件的生命周期钩子自然不会被触发。
为了解决这个问题，有以下3种解决方法：
### 17.3.1 路由导航守卫beforeRouteUpdate
beforeRouteUpdate守卫在当前路由改变且组件被复用时调用，所以可以在组件内定义路由导航守卫来解决问题。
组件的生命周期钩子虽然不会重新触发，但是路由提供的beforeRouteUpdate守卫可以被触发，所以只需要把每次切换路由时需要执行的逻辑放到beforeRouteUpdate守卫中即可。例如，在beforeRouteUpdate守卫中发送请求拉取数据，更新状态并重新渲染视图。
### 17.3.2 观察$route对象的变化
通过watch可以监听到路由对象发生的变化，从而对路由变化做出响应。
const User = {
  template: '...',
  watch: {
    '$route' (to, from) { // 这个是不好的做法
      // 请求个人信息
      // 请求列表
    }
  }
}
这种做法就是多了一个watch的内存开销。
如果最终选择使用watch，建议只观察自己需要的query参数，有利于减少不必要的请求。
例如页面中有个人信息和翻页信息，这时只需要观察翻页信息就行。
const User = {
  template: '...',
  watch: {
    '$route.query.id' () {
      // 请求个人信息
    },
    '$route.query.page' () {
      // 请求列表
    }
  }
}
### 17.3.3 为router-view组件添加属性key
这种做法简单粗暴，但是有效。因为通过给router-view组件设置key，可以使每次切换路由时的key都不一样，让虚拟DOM认为router-view组件是一个新节点，从而先销毁组件，然后再重新创建新组件，这种弊端就是每次切换组件时都会先销毁然后重建，非常浪费性能。
<router-view :key="$route.fullPath"></router-view>

## 17.4 为所有路由统一添加query
假设所有路由的后面都统一添加指定的query参数信息，但是router-view没有提供相应的api来处理这种情况。
### 17.4.1 使用全局守卫beforeEach
事实上，全局守卫beforeEach的并不具备修改query的能力，但是可以通过beforeEach的其中的next方法来中断当前导航，并切换到新导航，添加一些新query进去。
const query = {refferer: 'hao360cn'}
router.beforeEach((to, from, next) => {
  to.query.referer ? next() : next({...to, query: {...to.query, ...query}})
})
这种方式的优点是，可以全局统一配置公共的query参数，并且在组件内切换路由时无须进行特殊处理。缺点是每次切换路由时，全局守卫beforeEach会执行两次，即每次切换路由其实是切换两次。
### 17.4.2 使用函数劫持
原理是：通过拦截router.history.transitionTo方法，在vue-router内部在切换路由之前将参数添加到query中。这种做法虽可行，但是很危险的操作
const query = {referer: 'hao360cn'}
const transitionTo = router.history.transitionTo

router.history.transitionTo = function (location, onComplete, onAbort) {
  location = typeof location === 'object'
    ? {...location, query: {...location.query, ...query}}
    : {path: location, query}

  transitionTo.call(router.history, location, onComplete, onAbort)
}
## 17.5 区分Vuex与props的使用边界
业务组件一般会通过使用Vuex来维护状态，而通用组件会使用props来进行父子组件通信。
## 17.6 避免v-if和v-for一起使用
为了过滤一个列表中的项目（比如v-for="user in users"，v-if="user.isActive"）,请将users替换为一个计算属性（比如activeUsers），让它返回过滤后的列表。
为了避免渲染本应该被隐藏的列表（比如v-for="user in users" v-if="shouldShowUsers"），请将v-if移动至容器元素上（比如ul和ol）
vue.js处理指令时，v-for比v-if更高效，所以及时我们只渲染出列表中的一小部分元素，也得在每次重新渲染的时候遍历整个列表，而不考虑活跃用户是否发生了变化。通过将列表更换为一个计算属性遍历并过滤不需要渲染的数据，我们可以得到以下的好处：
1.过滤后的列表只会在数组发生相关变化时才会被重新计算，过滤更高效。
2.使用v-for="user in activateUsers"之后，我们在渲染时只遍历活跃用户，渲染更高效。
3.解耦渲染层的逻辑，可维护性更强。
<ul>
  <li 
    v-for="user in users"
    v-if="user.isActive"
    :key="user.id"
  >
    {{user.name}}
  </li>
</ul>
computed: {
  activeUsers: function () {
    return this.users.filter(function (user) {
      return user.isActive
    })
  }
}
<ul>
 <li 
  v-for="user in activeUsers"
  :key="user.id"
  >
  {{user.name}}
 </li>
</ul>
第二种情况就是：
shouldShowUsers移到最外层容器，这样就可以少做一次判断
<ul v-if="shouldShowUsers">
  <li v-for="user in users"
    :key="user.id"
  >
    {{user.name}}
  </li>
</ul>

## 17.7 为组件样式设置作用域
可以使用scroped特性或CSS Modules
## 17.8 避免在scroped中使用元素选择器
在scroped样式中，类选择器比元素选择器更好
## 17.9 避免隐形的父子组件通信
我们应该优先通过prop和事件进行父子组件之间的通信,而不是使用this.$parent或改变prop.
## 17.10 单文件组件如何命名
### 17.10.1单文件组件的文件名的大小写
单文件组件的命名应该是首字母大写也就是驼峰式命名,或者始终用短横线连接的命名.
### 17.10.2 基础组件名
### 17.10.3 单例组件名
### 17.10.4 紧密耦合的组件名
在单文件组件和字符串模板中用驼峰式命名组件,在DOM模板中用短横线命名方式.
## 17.11 自闭合组件
## 17.12 prop名的大小写
在JavaScript中使用驼峰式命名,在HTML中使用短横线命名
## 17.13 多个特性的元素
## 17.14 模板中简单的表达式

